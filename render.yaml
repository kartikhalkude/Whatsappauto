services:
  - type: web
    name: wpp
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    buildpacks:
      - heroku/nodejs
